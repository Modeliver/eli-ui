import React from "react";
import { HelperText as WMHelperText } from "@windmill/react-ui";

type HelperTextProps = {};

export const HelperText = (props: HelperTextProps) => {
  return <WMHelperText {...props} />;
};
